<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Message_app" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Đặt lại các màu bạn muốn thay thế. -->
        <item name="colorPrimary">@android:color/black</item>
    </style>

    <style name="Theme.Message_app" parent="Base.Theme.Message_app" />
</resources>
